type Query {
  countries: [Country!]!
  country(code: ID!): Country
  continent(code: ID!): Continent
}

type Country {
  code: ID!
  name: String!
  capital: String
  emoji: String!
  currency: String
  continent: Continent!
}

type Continent {
  code: ID!
  name: String!
}
